# Beekeeper Studio Minimal 源码打包说明

更新日期：2026-09-04

本文说明如何从当前项目源码生成可安装的 **macOS Apple Silicon（M 系列芯片）DMG**，以及如何修改图标、排查构建问题和压缩交付源码。

当前定制版已移除 Upgrade 按钮、升级弹窗及 AI 功能，采用石墨黑与蜂蜜金的极简图标。安装后的名称为 **Beekeeper Studio Minimal**，当前版本为 **5.8.1**。

## 1. 环境准备

| 项目 | 要求 / 当前构建记录 |
| --- | --- |
| 构建电脑 | Mac M 系列芯片；当前已验证 macOS 26.6.2、arm64 |
| Node.js | 优先使用 `.nvmrc` 指定的 **22.22.0**；本次实际使用 **22.18.0** 构建成功 |
| Yarn | **1.22.22**，由根目录 `package.json` 的 `packageManager` 固定 |
| Electron | **39.8.5**，由项目依赖安装，不必全局安装 |
| electron-builder | 本次锁文件解析版本 **26.15.3**，使用项目内版本 |
| 编译工具 | Xcode 或可用的 Command Line Tools、Python 3、Clang |
| 系统工具 | macOS 自带的 `iconutil`、`codesign`、`hdiutil` |

依赖安装和首次构建需要网络。图标生成会启动一个隐藏的 Electron 窗口，请在正常登录的 macOS 桌面会话中执行。

下面的命令默认从项目根目录执行。本机位置如下；换电脑后将 `cd` 路径改成实际的源码目录。

```bash
cd /Users/liuchanglong/OpenSource/beekeeper-studio

uname -m
xcode-select -p
clang --version
python3 --version
```

`uname -m` 应输出 `arm64`。如果尚未安装编译工具，执行 `xcode-select --install`，按系统提示安装完成后再继续。

如果已经安装 nvm，使用项目指定的 Node 版本：

```bash
nvm install
nvm use
node --version
```

启用项目要求的 Yarn：

```bash
corepack enable
corepack prepare yarn@1.22.22 --activate
yarn --version
```

如果已能运行 Yarn 1.22.22，可以跳过 Corepack 两步。不要同时使用 npm 和 Yarn 安装这个项目的依赖，以免产生不同的依赖树。

## 2. 最短打包流程

环境准备完成后，在项目根目录运行：

```bash
# 图标生成需要正常启动 Electron，不能以 Node 模式运行。
unset ELECTRON_RUN_AS_NODE

# 当前定制版采用本地签名，无需自动查找开发者证书。
export CSC_IDENTITY_AUTO_DISCOVERY=false

# 安装锁定版本的依赖，构建工具属于开发依赖，也需要安装。
yarn install --frozen-lockfile --production=false

# 生成图标、编译源码、制作 DMG。
yarn bks:build:mac:minimal
```

每一步成功后再执行下一步。依赖安装或原生模块重建失败时，先看第 7 节，不要直接忽略错误继续打包。

已经成功安装依赖、后续只修改源码时，通常只需要重新执行：

```bash
yarn bks:build:mac:minimal
```

这一命令依次完成：

1. 从 SVG 源文件生成 PNG、ICO 和 ICNS 图标。
2. 构建 `@beekeeperstudio/ui-kit` 组件库。
3. 用 esbuild 编译主进程、预加载脚本与数据库工作进程，用 Vite 编译界面。
4. 使用 `electron-builder-minimal.js` 打包 ARM64 应用，执行本地签名并生成 DMG。

构建命令包含 `--publish never`，不会上传或发布安装包。

## 3. 输出文件与安装

当前版本的输出目录：

```text
/Users/liuchanglong/OpenSource/beekeeper-studio/apps/studio/dist_electron/
├── Beekeeper-Studio-Minimal-5.8.1-arm64.dmg
├── Beekeeper-Studio-Minimal-5.8.1-arm64.dmg.blockmap
└── mac-arm64/
    └── Beekeeper Studio Minimal.app
```

| 文件 | 用途 |
| --- | --- |
| `.dmg` | 交付给用户的安装包，当前约 241 MB，后续构建大小可能变化 |
| `.app` | 已打包的 macOS 应用，可用于启动验证 |
| `.blockmap` | 打包工具生成的增量更新辅助文件，手动安装不需要它 |
| `.dmg.sha256` | 可选的校验文件，第 6 节提供生成方法；构建命令不会自动生成它 |

双击 DMG，将 **Beekeeper Studio Minimal** 拖入 **Applications / 应用程序** 即可安装。如果已经安装旧的 Minimal 版本，先退出旧应用，再替换安装。

当前配置使用 **ad-hoc 本地签名**，没有执行 Apple 公证，适用于本次本地定制交付。需要作为公开发行版分发时，应另行配置 Developer ID 签名及公证。

当前命令固定生成 Mac ARM64 安装包；Windows、Intel Mac 与 Linux 的安装包尚未在此流程中验证。

## 4. 分步构建与开发调试

需要定位某一个构建环节的问题时，可按顺序单独执行：

```bash
# 1. 图标
yarn workspace beekeeper-studio icons:build

# 2. UI 组件库
yarn lib:build

# 3. 应用主程序及界面
yarn workspace beekeeper-studio build

# 4. 安装包
yarn workspace beekeeper-studio electron-builder \
  --config electron-builder-minimal.js \
  --mac --arm64 --publish never
```

第 3 步只生成应用编译产物；需要执行第 4 步才会得到 DMG。组件库产物位于 `apps/ui-kit/dist`，应用编译产物位于 `apps/studio/dist`，安装包位于 `apps/studio/dist_electron`，均相对于项目根目录。

本地开发、查看界面修改效果使用：

```bash
yarn bks:dev
```

极简版应使用 `bks:build:mac:minimal` 或上面的分步命令。通用 `bks:build` 使用另一套构建配置，签名、公证等行为与此文档不同。

## 5. 修改图标、名称与版本

| 需要修改的内容 | 文件与说明 |
| --- | --- |
| 图标图形与颜色 | [SVG 源文件](/Users/liuchanglong/OpenSource/beekeeper-studio/apps/studio/src/assets/logo.svg)，当前为石墨黑 `#242725` 与蜂蜜金 `#EAC477` |
| 图标导出方式 | [图标生成脚本](/Users/liuchanglong/OpenSource/beekeeper-studio/apps/studio/build/generate-icons.cjs) |
| 应用名称、应用标识、安装包命名 | [极简版打包配置](/Users/liuchanglong/OpenSource/beekeeper-studio/apps/studio/electron-builder-minimal.js) |
| 应用版本号 | [应用 package.json](/Users/liuchanglong/OpenSource/beekeeper-studio/apps/studio/package.json) 中的 `version`；根目录的版本号不是应用版本号 |
| 默认设置 | [default.config.ini](/Users/liuchanglong/OpenSource/beekeeper-studio/apps/studio/default.config.ini) |
| 公共资源与原生依赖的打包规则 | [基础打包配置](/Users/liuchanglong/OpenSource/beekeeper-studio/apps/studio/electron-builder-config.js) |

只修改 SVG 后，运行 `yarn bks:build:mac:minimal` 即可同步生成所有图标并重打安装包。生成的图标包括：

- `public/icons/png/` 下 16、24、32、48、64、96、128、256、512、1024 像素的 PNG。
- `public/icons/mac/bk-icon.icns`，供 macOS 应用使用。
- `public/icons/win/favicon.ico`，供后续 Windows 构建使用。

以上路径均相对于 `apps/studio`。不要只替换一个 PNG；Dock 使用的是 ICNS，界面还会使用 SVG。

极简版当前的重要配置：

```javascript
appId: 'io.beekeeperstudio.desktop.minimal'
productName: 'Beekeeper Studio Minimal'
artifactName: 'Beekeeper-Studio-Minimal-${version}-${arch}.${ext}'
mac.target: [{ target: 'dmg', arch: ['arm64'] }]
mac.identity: '-'
mac.notarize: false
publish: null
```

上面是字段摘要，不是可以整体粘贴的 JavaScript 配置文件。实际编辑时保留现有文件结构，并保持 `productName` 与 `extraMetadata.productName` 一致。

默认配置中的 `checkForUpdatesDisabled = true` 用于关闭上游应用更新检查，避免定制版被上游版本替换。它不代表关闭所有第三方插件的更新。

## 6. 打包完成后如何验证

在项目根目录检查签名与 DMG 完整性。以下示例自动读取当前应用版本号：

```bash
BKS_VERSION="$(node -p "require('./apps/studio/package.json').version")"
BKS_DMG="apps/studio/dist_electron/Beekeeper-Studio-Minimal-${BKS_VERSION}-arm64.dmg"
BKS_APP="apps/studio/dist_electron/mac-arm64/Beekeeper Studio Minimal.app"

codesign --verify --deep --strict --verbose=2 "$BKS_APP"
hdiutil verify "$BKS_DMG"

# 确认应用包里的图标与本次生成的图标一致。
cmp apps/studio/public/icons/mac/bk-icon.icns \
  "$BKS_APP/Contents/Resources/icon.icns"
```

签名检查应成功退出，DMG 检查应出现 `VALID`，`cmp` 一致时不会输出内容。签名校验通过表示本地签名完整，不等同于通过 Apple 公证。

生成 SHA-256 校验文件：

```bash
BKS_VERSION="$(node -p "require('./apps/studio/package.json').version")"
(
  cd apps/studio/dist_electron || exit 1
  BKS_FILE="Beekeeper-Studio-Minimal-${BKS_VERSION}-arm64.dmg"
  shasum -a 256 "$BKS_FILE" > "$BKS_FILE.sha256"
  shasum -a 256 -c "$BKS_FILE.sha256"
)
```

重新生成 DMG 后也要重新生成校验文件，旧哈希不适用于新安装包。

启动应用后，建议复制项目自带的演示数据库到临时目录，用副本检查连接与查询：

```bash
BKS_DEMO_DIR="$(mktemp -d /private/tmp/beekeeper-demo.XXXXXX)"
cp apps/studio/extra_resources/demo.db "$BKS_DEMO_DIR/demo.db"
printf '%s\n' "$BKS_DEMO_DIR/demo.db"
```

在应用中选择 SQLite，等待连接表单加载完成，选择上面输出的数据库文件并连接，然后执行：

```sql
SELECT 1 AS minimal_build_check;
SELECT name FROM sqlite_master WHERE name = 'cheeses';
```

第一条应返回 `1`，第二条应返回 `cheeses`。同时确认界面没有 Upgrade 和 AI 入口，应用图标为新版极简图标。

本次已实际完成源码构建、原生模块重建和 SQLite 查询验证；图标更新后的安装包也已完成图标一致性、签名与 DMG 完整性检查。

## 7. 常见失败与处理

### 7.1 下载慢、超时或 Corepack 获取 Yarn 失败

网络正常时不必设置镜像。如果依赖、Electron 或打包工具下载超时，可在当前终端设置：

```bash
export npm_config_registry=https://registry.npmmirror.com
export ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
export ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/

yarn install --frozen-lockfile --production=false \
  --registry=https://registry.npmmirror.com --network-timeout 60000
```

如果 Corepack 无法下载 Yarn，可在临时目录安装同版本 Yarn，并仅调整当前终端的 PATH：

```bash
BKS_YARN_TOOLS="$(mktemp -d /private/tmp/beekeeper-yarn.XXXXXX)"
npm install --prefix "$BKS_YARN_TOOLS" --no-audit --no-fund \
  --registry=https://registry.npmmirror.com yarn@1.22.22
export PATH="$BKS_YARN_TOOLS/node_modules/.bin:$PATH"
yarn --version
```

这不会在项目中生成 `package-lock.json`。如果只有源码压缩包、没有 `.git`，遇到 Husky 安装钩子报错时可在当前终端设置 `export HUSKY=0`，再重新安装依赖。

### 7.2 安装依赖时卡在原生模块，或出现 Electron 头文件校验失败

本项目依赖 `better-sqlite3`、`kerberos`、`sqlanywhere` 等原生模块。它们必须与打包使用的 Electron 版本及 ARM64 架构匹配。

本次遇到过两类问题：官方头文件下载地址连接超时；镜像的 `SHASUMS256.txt` 没有对应头文件条目。处理方法是下载官方头文件及其校验清单，校验通过后用本地头文件重建。

先完成依赖下载。`--ignore-scripts` 只是将安装与原生编译拆开，使用它之后必须继续完成下面的 Electron 安装和原生模块重建。

```bash
yarn install --frozen-lockfile --production=false --ignore-scripts \
  --registry=https://registry.npmmirror.com --network-timeout 60000

node node_modules/electron/install.js
```

然后在项目根目录运行下面的完整代码块。任一步失败都会停止；所有环境设置只在这一子 Shell 内生效。修复与打包放在同一个代码块内，确保打包工具也能找到已校验的头文件。

```bash
(
  set -eu
  unset ELECTRON_RUN_AS_NODE
  export CSC_IDENTITY_AUTO_DISCOVERY=false
  export BKS_ELECTRON_VERSION="$(node -p "require('./node_modules/electron/package.json').version")"
  export BKS_HEADERS_DIR="$(mktemp -d /private/tmp/beekeeper-headers.XXXXXX)"
  BKS_HEADERS_URL="https://artifacts.electronjs.org/headers/dist/v${BKS_ELECTRON_VERSION}"
  BKS_HEADERS_FILE="node-v${BKS_ELECTRON_VERSION}-headers.tar.gz"

  curl --fail --location --retry 3 \
    "$BKS_HEADERS_URL/SHASUMS256.txt" \
    -o "$BKS_HEADERS_DIR/SHASUMS256.txt"
  curl --fail --location --retry 3 \
    "$BKS_HEADERS_URL/$BKS_HEADERS_FILE" \
    -o "$BKS_HEADERS_DIR/$BKS_HEADERS_FILE"

  python3 - <<'PY'
import hashlib
import os
from pathlib import Path

folder = Path(os.environ['BKS_HEADERS_DIR'])
name = f"node-v{os.environ['BKS_ELECTRON_VERSION']}-headers.tar.gz"
checksums = {}
for line in (folder / 'SHASUMS256.txt').read_text().splitlines():
    parts = line.split()
    if len(parts) == 2:
        checksums[parts[1].lstrip('*')] = parts[0]
expected = checksums.get(name)
actual = hashlib.sha256((folder / name).read_bytes()).hexdigest()
if expected is None or actual != expected:
    raise SystemExit('头文件校验失败，已停止；请检查下载来源和版本。')
print('官方 Electron 头文件 SHA-256 校验通过。')
PY

  tar -xzf "$BKS_HEADERS_DIR/$BKS_HEADERS_FILE" -C "$BKS_HEADERS_DIR"
  test -f "$BKS_HEADERS_DIR/node_headers/include/node/node.h"
  export npm_package_config_node_gyp_nodedir="$BKS_HEADERS_DIR/node_headers"

  yarn electron-rebuild --module-dir apps/studio \
    --version "$BKS_ELECTRON_VERSION" --arch arm64 --sequential

  yarn bks:build:mac:minimal
)
```

当前 Electron 39.8.5 的头文件解压结构是 `node_headers/include/node`。升级 Electron 后若结构变化，应依据实际解压结果调整 `nodedir`，并重新验证构建。

不要关闭 TLS 校验或跳过 SHA-256 校验来处理下载错误。

### 7.3 其他常见提示

| 现象 | 处理方法 |
| --- | --- |
| `NODE_MODULE_VERSION` 不匹配、原生 `.node` 文件无法加载 | 用当前 Electron 版本执行上面的原生重建；不要仅使用系统 Node 执行 `npm rebuild` |
| `Electron failed to install correctly` | 运行 `node node_modules/electron/install.js`，确认网络或镜像设置可用 |
| 无开发者证书、Apple 公证凭据不足 | 检查是否使用 `electron-builder-minimal.js`；当前本地版本的 `identity` 为 `'-'`，`notarize` 为 `false` |
| 提示 ad-hoc 签名需要关闭 library validation | 当前 [entitlements.mac.plist](/Users/liuchanglong/OpenSource/beekeeper-studio/apps/studio/build/entitlements.mac.plist) 已包含相应 entitlement；最终以应用启动及签名校验结果为准 |
| `file source doesn't exist ... node_modules/ws` | 当前基础配置已用 `require.resolve('ws/package.json')` 定位 Yarn 提升后的依赖；确认使用包含此修复的源码，并重新安装依赖 |
| SVG 已修改，Dock 图标仍旧 | 重新生成图标并打包，退出已安装的旧应用后替换安装；用第 6 节的 `cmp` 核对新 `.app` 的图标 |
| Vite 提示 chunk 较大，或 Sass 提示弃用 | 本次构建存在这些提示但成功完成；应关注退出码及后续是否生成 DMG |

需要保留构建日志时，可在项目根目录执行：

```bash
yarn bks:build:mac:minimal > /private/tmp/beekeeper-minimal-build.log 2>&1
```

失败时从日志中寻找第一个实际错误，不要只看最后一行 Yarn 的退出码。

## 8. 源码压缩交付

如果要把源码交给另一台电脑或其他开发者，应交付当前工作目录中的源码、`yarn.lock`、根目录与子项目的 `package.json`、`.nvmrc`、`.yarn` 下的项目文件、图标及打包配置。

`node_modules`、编译输出和 DMG 可以重新生成，不必放入源码压缩包。`apps/ui-kit/lib` 是组件库源码，必须保留；不要按目录名把所有 `lib` 或 `vendor` 都排除。

下面命令从项目根目录生成源码压缩包，包含当前尚未提交的修改。目标文件位于项目的上一级目录，避免把压缩包自身再次收进包里。

```bash
BKS_SOURCE_ARCHIVE="../beekeeper-studio-minimal-source-$(date +%Y%m%d-%H%M%S).tar.gz"
COPYFILE_DISABLE=1 tar \
  --exclude='./.git' \
  --exclude='node_modules' \
  --exclude='.DS_Store' \
  --exclude='./apps/studio/dist' \
  --exclude='./apps/studio/dist_electron' \
  --exclude='./apps/ui-kit/dist' \
  --exclude='./apps/studio/tmp' \
  --exclude='./apps/studio/coverage' \
  --exclude='./apps/ui-kit/coverage' \
  -czf "$BKS_SOURCE_ARCHIVE" .
```

交付前检查是否混入个人连接配置、测试数据库或凭据文件，按实际情况排除。此命令是工作目录压缩，不会自动识别所有私人文件。

另一台 Mac 解压后，从第 1 节开始准备环境并构建即可。若使用 `git archive` 交付，需要先确认定制修改已提交，因为它不会包含未提交文件。
